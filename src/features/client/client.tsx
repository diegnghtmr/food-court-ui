import { RestaurantsGrid } from './components/RestaurantsGrid'

/**
 * Client Module Container
 * Renders the main client view (restaurants grid)
 * Child routes use the layout's Outlet
 */
export const Client = () => {
  return <RestaurantsGrid />
}
